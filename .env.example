# Database
SSL=sslmode=disable

DB_USER=postgres
DB_PASS=password

DEV_NAME=charity_dev
DEV_PORT=5434
TEST_NAME=charity_test
TEST_PORT=5435

DB_URL=postgres://${DB_USER}:${DB_PASS}@localhost:${DEV_PORT}/${DEV_NAME}?${SSL}
DB_TEST_URL=postgres://${DB_USER}:${DB_PASS}@localhost:${TEST_PORT}/${TEST_NAME}?${SSL}

# App
APP_PORT=1234

# JWT
JWT_SECRET=replace-me-with-a-generated-secret
